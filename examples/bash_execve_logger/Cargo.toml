[package]
name = "bash-execve-logger"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
funchook = { path = "../.." }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
strip = "debuginfo"
